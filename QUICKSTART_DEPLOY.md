# ⚡ Быстрый старт: Деплой на Vercel

## За 10 минут запустите сайт на домене hanplaza-docs.ru

> **🚀 Быстрый старт для опытных:**
> 1. Создайте PostgreSQL на neon.tech
> 2. Деплой на vercel.com → добавьте `DATABASE_URL` и `JWT_SECRET`
> 3. После деплоя создайте админа через curl (см. шаг 5️⃣)

### 1️⃣ Создайте базу данных PostgreSQL (2 мин)

1. Зайдите на **[neon.tech](https://neon.tech)** → Sign up (бесплатно)
2. Create Project → назовите "han-plaza"
3. Скопируйте **Connection String** (начинается с `postgresql://`)

### 2️⃣ Загрузите код на GitHub (3 мин)

```bash
# В папке проекта
git init
git add .
git commit -m "Initial commit"

# Создайте репозиторий на github.com
# Затем:
git remote add origin https://github.com/ВАШ-USERNAME/han-plaza.git
git branch -M main
git push -u origin main
```

### 3️⃣ Деплой на Vercel (3 мин)

1. Зайдите на **[vercel.com](https://vercel.com)** → Sign up с GitHub
2. **Add New Project** → Import вашего репозитория
3. **Environment Variables** → добавьте:
   ```
   DATABASE_URL = ваш postgresql url от Neon
   JWT_SECRET = любая случайная строка (мин 32 символа)
   ```
4. **Deploy!** ✅

> ⏰ **Подождите 2-3 минуты** пока Vercel соберет и задеплоит приложение

### 4️⃣ Настройка домена (2 мин)

**В Vercel:**
1. Settings → Domains → Add Domain
2. Введите `hanplaza-docs.ru`
3. Скопируйте DNS записи

**У регистратора домена:**
1. Перейдите в управление DNS
2. Добавьте записи от Vercel:
   ```
   Type: A
   Name: @
   Value: 76.76.21.21
   
   Type: CNAME
   Name: www
   Value: cname.vercel-dns.com
   ```
3. Сохраните (применится через 5-30 мин)

### 5️⃣ Запустите миграции и создайте админа

> **💡 Совет:** Vercel автоматически применяет миграции при деплое. Нужно только создать админа!

**Способ 1: Через curl (САМЫЙ ПРОСТОЙ)** ⭐
```bash
# Замените URL на ваш реальный домен
curl -X POST https://ваш-сайт.vercel.app/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@hanplaza.ru",
    "password": "password123",
    "name": "Администратор",
    "role": "admin"
  }'
```

**Способ 2: Через терминал (локально)**
```bash
# 1. Установите зависимости локально (если еще не установлены)
npm install

# 2. Примените миграции к продакшн базе
DATABASE_URL="ваш_postgresql_url_от_neon" npx prisma migrate deploy

# 3. Создайте начальных пользователей
DATABASE_URL="ваш_postgresql_url_от_neon" npx prisma db seed
```

**Способ 3: Через Postman или браузер**
1. Откройте Postman или любой REST клиент
2. Создайте POST запрос:
   - URL: `https://ваш-сайт.vercel.app/api/auth/register`
   - Headers: `Content-Type: application/json`
   - Body:
     ```json
     {
       "email": "admin@hanplaza.ru",
       "password": "password123",
       "name": "Администратор",
       "role": "admin"
     }
     ```
3. Отправьте запрос

**Способ 4: Через консоль браузера**
1. Откройте ваш сайт в браузере
2. Нажмите F12 → Console
3. Вставьте и выполните:
   ```javascript
   fetch('https://ваш-сайт.vercel.app/api/auth/register', {
     method: 'POST',
     headers: { 'Content-Type': 'application/json' },
     body: JSON.stringify({
       email: 'admin@hanplaza.ru',
       password: 'password123',
       name: 'Администратор',
       role: 'admin'
     })
   }).then(r => r.json()).then(console.log)
   ```

### 6️⃣ Готово! 🎉

Сайт доступен на:
- ✅ `https://hanplaza-docs.ru`
- ✅ `https://www.hanplaza-docs.ru`

**Войдите как админ:**
- Email: `admin@hanplaza.ru`
- Пароль: `password123`

⚠️ **Сразу смените пароль в админ-панели!**

---

## Если что-то пошло не так

### Ошибка "Environment Variable references Secret which does not exist"
**Проблема:** Vercel пытается использовать синтаксис секретов вместо прямого значения.

**Решение:**
1. Зайдите в **Vercel Dashboard** → ваш проект → **Settings** → **Environment Variables**
2. Найдите переменную `DATABASE_URL`
3. Если в поле стоит что-то вроде `@database_url` - это неправильно!
4. **Удалите** эту переменную (нажмите на три точки → Delete)
5. **Создайте заново**: 
   - Key: `DATABASE_URL`
   - Value: вставьте полный connection string от Neon (например: `postgresql://user:password@host.neon.tech/dbname`)
   - Environments: выберите все (Production, Preview, Development)
6. **Redeploy**: Settings → Deployments → последний деплой → три точки → Redeploy

### Ошибка "Invalid DATABASE_URL"
- Проверьте что скопировали полный URL с паролем
- URL должен начинаться с `postgresql://`
- В Vercel проверьте Environment Variables

### "To deploy to production, push to the Repository Default branch"
**Проблема:** Vercel ждет код в репозитории для деплоя.

**Решение:**

1. **Проверьте что код загружен на GitHub:**
   ```bash
   git status  # проверьте текущее состояние
   git log     # убедитесь что есть коммиты
   ```

2. **Если код еще не загружен, выполните:**
   ```bash
   git add .
   git commit -m "Initial commit"
   git push -u origin main
   ```

3. **Если код уже на GitHub, но деплой не запускается:**
   - Вариант А: Сделайте пустой коммит для триггера деплоя:
     ```bash
     git commit --allow-empty -m "Trigger deployment"
     git push
     ```
   - Вариант Б: В Vercel Dashboard → Deployments → **Redeploy** (на последнем деплое)

4. **Проверьте основную ветку:**
   ```bash
   git branch  # должна быть main или master
   ```
   - В Vercel Settings → Git → убедитесь что Production Branch совпадает с вашей основной веткой

### Домен не работает
- Подождите 15-30 минут (DNS propagation)
- Проверьте DNS записи: `nslookup hanplaza-docs.ru`

### Ошибка 500 после деплоя
- Проверьте логи в Vercel (Deployments → Logs)
- Убедитесь что миграции применены (см. ниже)
- Убедитесь что JWT_SECRET задан

### Миграции не применились автоматически
**Решение:** Примените вручную через терминал
```bash
# Установите зависимости
npm install

# Примените миграции
DATABASE_URL="ваш_postgresql_url_от_neon" npx prisma migrate deploy
```

### "Functions require a paid plan" или не можете найти Functions
**Это нормально!** Functions - платная фича Vercel. Используйте **Способ 1** из раздела 5️⃣ (создание админа через curl API) - это бесплатно и работает сразу после деплоя.

### CORS ошибка "Missing Allow Origin"
**Проблема:** Браузер блокирует запросы из-за политики CORS.

**Решение:** Добавлена поддержка CORS в последней версии кода. 

**Если получили эту ошибку:**
1. Убедитесь что используете последнюю версию кода
2. Закоммитьте и запушьте изменения:
   ```bash
   git add .
   git commit -m "Add CORS support"
   git push
   ```
3. Или используйте **Способ 1 (curl)** - он не требует CORS

**Альтернатива:** Используйте curl из терминала вместо браузера:
```bash
curl -X POST https://ваш-сайт.vercel.app/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@hanplaza.ru","password":"password123","name":"Администратор","role":"admin"}'
```

---

## Полная документация

Смотрите [DEPLOY.md](./DEPLOY.md) для:
- Альтернативных вариантов хостинга
- Настройки VPS
- Railway деплой
- Troubleshooting

---

**Нужна помощь?** Проверьте логи в Vercel или создайте Issue на GitHub.

