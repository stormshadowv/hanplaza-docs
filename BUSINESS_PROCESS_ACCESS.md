# 🔒 Ролевой доступ к бизнес-процессам

## Обзор

Система контроля доступа к бизнес-процессам позволяет ограничивать видимость процессов на основе ролей пользователей. Это обеспечивает безопасность и предотвращает доступ к конфиденциальной информации.

## Как это работает

Каждый бизнес-процесс имеет поле `allowedRoles`, которое определяет, какие роли пользователей могут просматривать этот процесс.

### Матрица доступа

| Роль пользователя | Видимые процессы |
|-------------------|------------------|
| `admin` | **Все процессы** |
| `manager` | Процесс продажи, Обработка рекламации |
| `buyer` | Процесс закупки товара |
| `customer-service` | Обработка рекламации |
| `warehouse` | Процессы, где участвует склад |
| `logistics` | Процессы, где участвует логистика |

## Защита на уровне API

### GET /api/processes

Возвращает только те процессы, к которым у пользователя есть доступ:

```typescript
// Автоматическая фильтрация по ролям
if (user.role === 'admin') {
  return allProcesses // Админы видят всё
}

return processes.filter(process => {
  if (!process.allowedRoles || process.allowedRoles === 'all') {
    return true // Доступно всем
  }
  return process.allowedRoles.includes(user.role)
})
```

### GET /api/processes/[id]

Проверяет права доступа перед возвратом конкретного процесса:

```typescript
// Если пользователь не админ и процесс имеет ограничения
if (user.role !== 'admin' && process.allowedRoles) {
  const allowedRolesArray = process.allowedRoles.split(',')
  if (!allowedRolesArray.includes(user.role)) {
    return 403 Forbidden
  }
}
```

## Примеры использования

### Пример 1: Процесс только для менеджеров

```json
{
  "name": "Процесс продажи",
  "description": "Полный цикл работы с клиентом",
  "allowedRoles": "manager,admin"
}
```

✅ Видят: менеджеры и админы  
❌ Не видят: закупщики, складские работники и др.

### Пример 2: Процесс для нескольких ролей

```json
{
  "name": "Обработка рекламации",
  "description": "Работа с жалобами клиентов",
  "allowedRoles": "customer-service,manager,admin"
}
```

✅ Видят: клиентский сервис, менеджеры, админы  
❌ Не видят: закупщики, дизайнеры и др.

### Пример 3: Процесс доступный всем

```json
{
  "name": "Общий процесс",
  "description": "Доступен всем сотрудникам",
  "allowedRoles": "all"
}
```

или

```json
{
  "name": "Общий процесс",
  "allowedRoles": ""
}
```

✅ Видят: все авторизованные пользователи

## Создание процесса с ограничениями

### Через админ-панель

1. Войдите как админ (`admin@hanplaza.com`)
2. Перейдите в админ-панель `/admin`
3. Вкладка "Бизнес-процесс"
4. Заполните форму:
   - Название процесса
   - Описание
   - Отделы (через запятую)
   - **Доступные роли** - выберите из списка
5. Нажмите "Добавить бизнес-процесс"

### Через API

```bash
curl -X POST http://localhost:3000/api/processes \
  -H "Authorization: Bearer ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Новый процесс",
    "description": "Описание процесса",
    "departments": ["Отдел продаж", "Логистика"],
    "allowedRoles": "manager,admin",
    "steps": []
  }'
```

## Доступные роли

- `admin` - Администраторы (видят всё)
- `manager` - Менеджеры по продажам
- `buyer` - Закупщики
- `warehouse` - Складские работники
- `designer` - Дизайнеры
- `logistics` - Логисты
- `customer-service` - Обслуживание клиентов

## Безопасность

✅ **Защита на уровне API** - невозможно обойти через фронтенд  
✅ **JWT токены** - аутентификация и авторизация  
✅ **Роли хранятся в токене** - проверка на каждый запрос  
✅ **Фильтрация на сервере** - клиент не получает данные, к которым нет доступа  
✅ **Проверка прав на создание/изменение** - только админы могут управлять процессами

## Структура базы данных

```sql
CREATE TABLE BusinessProcess (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  description TEXT NOT NULL,
  departments TEXT NOT NULL, -- JSON array
  allowedRoles TEXT DEFAULT '', -- "manager,admin", "all" или "" для всех
  createdAt DATETIME DEFAULT CURRENT_TIMESTAMP,
  updatedAt DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

## Миграция существующих данных

Если у вас уже есть бизнес-процессы без поля `allowedRoles`:

```bash
# Применить миграцию
npx prisma migrate deploy

# Обновить seed данные
npm run db:seed
```

## FAQ

**Q: Что если allowedRoles пустой или равен "all"?**  
A: Процесс доступен всем авторизованным пользователям.

**Q: Может ли процесс быть виден нескольким ролям?**  
A: Да! Используйте запятую: `"manager,buyer,admin"`

**Q: Админы всегда видят всё?**  
A: Да, роль `admin` имеет доступ ко всем процессам, независимо от `allowedRoles`.

**Q: Можно ли скрыть процесс даже от админа?**  
A: Нет, текущая реализация всегда показывает всё админам.

**Q: Что происходит при попытке доступа к запрещенному процессу?**  
A: API возвращает ошибку 403 Forbidden.

**Q: Нужна ли авторизация для просмотра процессов?**  
A: Да, все endpoints `/api/processes/*` требуют валидный JWT токен.

## Тестирование

### 1. Войдите как менеджер

```
Email: manager@hanplaza.ru
Пароль: password123
```

✅ **Результат:** Видит "Процесс продажи" и "Обработка рекламации"

### 2. Войдите как закупщик

```
Email: buyer@hanplaza.ru
Пароль: password123
```

✅ **Результат:** Видит только "Процесс закупки товара"

### 3. Войдите как админ

```
Email: admin@hanplaza.com
Пароль: admin123
```

✅ **Результат:** Видит ВСЕ процессы

## Логирование и мониторинг

Все попытки доступа к процессам логируются:

```javascript
console.log(`User ${user.email} (${user.role}) accessed process ${processId}`)
```

Рекомендуется настроить мониторинг для отслеживания:
- Попыток несанкционированного доступа (403 ошибки)
- Частоты обращений к конфиденциальным процессам
- Изменений в правах доступа

## Обновление документации

При добавлении новых ролей или процессов обновите:
1. Этот файл (`BUSINESS_PROCESS_ACCESS.md`)
2. `ROLE_BASED_ACCESS.md` - общая документация по ролям
3. Seed данные (`prisma/seed.ts`)
4. Админ-панель (`app/admin/page.tsx`) - список ролей в селекторе

