# Han Plaza - Обучающая платформа

Полнофункциональная обучающая платформа с системой авторизации, управлением категориями и видео.

## 🌐 Деплой в продакшен

### Быстрый старт (10 минут)
👉 **[QUICKSTART_DEPLOY.md](./QUICKSTART_DEPLOY.md)** - Пошаговая инструкция для деплоя на Vercel с доменом

### Полная документация
📚 **[DEPLOY.md](./DEPLOY.md)** - Все варианты хостинга (Vercel, Railway, VPS)

---

## 🚀 Технологии

- **Frontend**: Next.js 16, React 19, TypeScript
- **UI**: shadcn/ui, Tailwind CSS
- **Backend**: Next.js API Routes
- **База данных**: SQLite с Prisma ORM
- **Авторизация**: JWT токены, bcryptjs

## 📦 Установка

### 1. Клонирование и установка зависимостей

```bash
# Установка зависимостей
npm install
```

### 2. Настройка базы данных

```bash
# Применение миграций
npx prisma migrate dev

# Заполнение базы данных начальными данными
npm run db:seed
```

### 3. Запуск приложения

```bash
# Режим разработки
npm run dev

# Сборка для продакшена
npm run build
npm start
```

Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000)

## 👥 Тестовые пользователи

После выполнения `npm run db:seed` будут созданы следующие пользователи:

| Email | Пароль | Роль | Видимые категории |
|-------|--------|------|-------------------|
| admin@hanplaza.ru | password123 | admin | ✅ Все категории + Админ-панель |
| manager@hanplaza.ru | password123 | manager | Только "Менеджер" |
| buyer@hanplaza.ru | password123 | buyer | Только "Закупщик" |
| warehouse@hanplaza.ru | password123 | warehouse | Только "Складской работник" |
| designer@hanplaza.ru | password123 | designer | Только "Дизайнер" |
| logistics@hanplaza.ru | password123 | logistics | Только "Логистика" |

**🔒 Ролевая система доступа:**
- Каждый пользователь видит только свою категорию
- Администраторы видят все категории
- Доступ контролируется на уровне API

## 📚 API Endpoints

### Авторизация

- `POST /api/auth/login` - Вход в систему
- `POST /api/auth/register` - Регистрация нового пользователя
- `GET /api/auth/me` - Получение информации о текущем пользователе

### Категории

- `GET /api/categories` - Получить все категории
- `POST /api/categories` - Создать категорию (только админ)

### Видео

- `GET /api/videos` - Получить все видео
- `GET /api/videos?category={slug}` - Получить видео по категории
- `GET /api/videos/{id}` - Получить конкретное видео
- `POST /api/videos` - Создать видео (только админ)
- `PATCH /api/videos/{id}` - Обновить видео (например, увеличить просмотры)

## 🔐 Авторизация

Для защищенных запросов используйте JWT токен в заголовке:

```
Authorization: Bearer {token}
```

## 🗄️ База данных

Проект использует SQLite с Prisma ORM. Файл базы данных: `prisma/dev.db`

### Схема базы данных

- **User** - Пользователи системы
- **Category** - Категории обучающих материалов
- **Video** - Видео материалы

### Полезные команды Prisma

```bash
# Открыть Prisma Studio (GUI для базы данных)
npx prisma studio

# Пересоздать базу данных
npx prisma migrate reset

# Создать новую миграцию
npx prisma migrate dev --name your_migration_name
```

## 📁 Структура проекта

```
├── app/                      # Next.js app directory
│   ├── admin/              # 👑 Админ-панель
│   ├── api/                # API routes
│   │   ├── auth/          # Авторизация
│   │   ├── categories/    # Категории
│   │   └── videos/        # Видео
│   ├── category/[id]/     # Страница категории
│   ├── dashboard/         # Главная страница
│   └── login/             # Страница входа
├── components/              # React компоненты
│   ├── ui/                 # shadcn/ui компоненты
│   ├── auth-guard.tsx      # Защита маршрутов
│   └── header.tsx          # Шапка сайта
├── lib/                     # Утилиты и библиотеки
│   ├── api-client.ts       # Клиент для API
│   ├── auth.ts             # Middleware авторизации
│   ├── jwt.ts              # Работа с JWT
│   ├── prisma.ts           # Prisma клиент
│   └── utils.ts            # Общие утилиты
├── prisma/                  # Prisma схема и миграции
│   ├── schema.prisma       # Схема базы данных
│   └── seed.ts             # Скрипт для заполнения БД
└── public/                  # Статические файлы
```

## 🎨 Особенности

- ✅ Полная система авторизации с JWT
- ✅ Защита маршрутов и API
- ✅ **Ролевая система доступа** - каждый видит только свои категории
- ✅ **Админ-панель** для управления контентом
- ✅ Добавление видео и категорий через UI
- ✅ CRUD операции для категорий и видео
- ✅ Счетчик просмотров видео
- ✅ Адаптивный дизайн
- ✅ Темная/светлая тема (через next-themes)
- ✅ TypeScript для type safety
- ✅ Валидация данных
- ✅ Фильтрация контента на уровне API

## 👑 Админ-панель

Доступна по адресу: **http://localhost:3000/admin**

### Возможности админки:

1. **Добавление видео**
   - Название и описание
   - Выбор категории
   - URL видео (YouTube embed)
   - Обложка видео
   - Длительность

2. **Добавление категорий**
   - Название и описание
   - Автогенерация slug
   - Выбор иконки
   - Просмотр существующих категорий

### Как попасть в админку:

1. Войдите как администратор (admin@hanplaza.ru)
2. Нажмите на иконку профиля в шапке
3. Выберите "Админ-панель"
4. Или перейдите напрямую: `/admin`

**Защита:** Только пользователи с ролью `admin` могут получить доступ!

## 🔒 Ролевая система доступа

Система автоматически фильтрует категории в зависимости от роли пользователя:

- **Менеджеры** видят только категорию "Менеджер"
- **Закупщики** видят только категорию "Закупщик"
- **Складские работники** видят только свою категорию
- **Администраторы** видят всё

### Как это работает:

1. Каждая категория имеет поле `allowedRoles` (роли через запятую)
2. При запросе категорий API проверяет роль пользователя из JWT токена
3. Возвращаются только те категории, к которым есть доступ
4. Защита реализована на уровне API - невозможно обойти

### Тестирование:

Войдите с разными пользователями и убедитесь, что каждый видит только свою категорию:

```bash
# Менеджер
manager@hanplaza.ru / password123
→ Видит только "Менеджер"

# Закупщик
buyer@hanplaza.ru / password123
→ Видит только "Закупщик"

# Админ
admin@hanplaza.ru / password123
→ Видит ВСЁ
```

Подробнее см. [ROLE_BASED_ACCESS.md](ROLE_BASED_ACCESS.md)

## 🔧 Разработка

### Добавление новых категорий

Вы можете добавлять категории через API или напрямую в `prisma/seed.ts` и перезапустить seed:

```bash
npm run db:seed
```

### Добавление новых видео

Используйте API endpoint `POST /api/videos` или добавьте в `prisma/seed.ts`

## 📝 Переменные окружения

Создайте файл `.env` в корне проекта (уже создан автоматически):

```env
DATABASE_URL="file:./dev.db"
JWT_SECRET="your-secret-key-change-in-production"
```

**Важно**: Измените `JWT_SECRET` в продакшене!

## 🚀 Деплой

### Vercel (рекомендуется)

1. Загрузите проект на GitHub
2. Импортируйте в Vercel
3. Настройте переменные окружения
4. Деплой!

**Примечание**: Для продакшена рекомендуется использовать PostgreSQL вместо SQLite.

## 📄 Лицензия

MIT

## 👨‍💻 Автор

Создано для Han Plaza

