# Руководство по обновлению проекта

## Что нового в этом обновлении

### 🎨 Новый дизайн
- Обновлен дизайн главной страницы с двумя секциями: "Бизнес-процессы" и "Обучающие материалы"
- Добавлены визуальные улучшения с иконками и hover-эффектами
- Улучшена навигация с breadcrumbs

### 📊 Новые функции

#### 1. Бизнес-процессы
Новая функциональность для отображения и управления бизнес-процессами компании:
- Карточки процессов на главной странице
- Детальная страница процесса с шагами
- Визуальная временная линия с нумерованными этапами
- Связь шагов с обучающими материалами

#### 2. Контент (статьи и инструкции)
Расширенная система контента:
- Поддержка статей с Markdown форматированием
- Пошаговые инструкции
- Страница просмотра контента с красивым рендерингом

### 🗄️ Изменения в базе данных

Добавлены три новые таблицы:

1. **Content** - для хранения статей и инструкций
2. **BusinessProcess** - для бизнес-процессов
3. **ProcessStep** - для шагов процессов

## Как обновить существующий проект

### Шаг 1: Обновить зависимости
```bash
npm install react-markdown
```

### Шаг 2: Применить миграции
```bash
# Сгенерировать Prisma Client
npx prisma generate --schema=prisma/schema.postgresql.prisma

# Применить миграции к базе данных
npx prisma migrate deploy --schema=prisma/schema.postgresql.prisma
```

### Шаг 3: Заполнить базу тестовыми данными
```bash
npm run db:seed
```

### Шаг 4: Проверить статус миграций
```bash
npx prisma migrate status --schema=prisma/schema.postgresql.prisma
```

Должно вывести: "Database schema is up to date!"

### Шаг 5: Запустить проект
```bash
npm run dev
```

## Новые файлы

### Страницы
- `app/process/[id]/page.tsx` - страница бизнес-процесса
- `app/content/[id]/page.tsx` - страница контента (статьи/инструкции)

### Обновленные файлы
- `lib/data.ts` - добавлены типы и моковые данные для новых функций
- `app/dashboard/page.tsx` - обновлен дизайн с двумя секциями
- `prisma/schema.postgresql.prisma` - добавлены новые модели
- `prisma/seed.ts` - добавлено заполнение новых таблиц

### Миграции
- `20251224200338_init` - начальная миграция
- `20251224200416_add_content_and_business_processes` - добавление новых таблиц

## Структура данных

### BusinessProcess
```typescript
{
  id: string
  name: string
  description: string
  departments: string[] // JSON array
  steps: ProcessStep[]
}
```

### ProcessStep
```typescript
{
  id: number
  title: string
  description: string
  responsible: string
  duration?: string
  relatedContentIds?: string[] // JSON array
}
```

### Content
```typescript
{
  id: string
  title: string
  description: string
  categoryId: string
  type: "video" | "article" | "instruction"
  content?: string // Markdown
  views: number
}
```

## Тестовые данные

После выполнения `npm run db:seed` будут созданы:

### Бизнес-процессы (4 шт):
1. Процесс продажи (6 шагов)
2. Процесс закупки товара (6 шагов)
3. Обработка рекламации (6 шагов)
4. Адаптация нового сотрудника (6 шагов)

### Контент (2 шт):
1. Статья: "Техники активных продаж"
2. Инструкция: "Работа в CRM системе"

## Проверка работы

После обновления проверьте:

1. ✅ Главная страница отображает две секции
2. ✅ Карточки бизнес-процессов кликабельны
3. ✅ Страница процесса показывает шаги с временной линией
4. ✅ Связанные материалы отображаются в шагах
5. ✅ Страница контента рендерит Markdown

## Возможные проблемы

### Ошибка "Content model not found"
Убедитесь, что выполнили:
```bash
npx prisma generate --schema=prisma/schema.postgresql.prisma
```

### Ошибка "react-markdown not found"
Установите зависимость:
```bash
npm install react-markdown
```

### Миграции не применяются
Проверьте `DATABASE_URL` в `.env` и убедитесь, что используете PostgreSQL схему.

## Документация

Подробная информация о миграциях доступна в файле `MIGRATIONS.md`.

## Поддержка

При возникновении проблем проверьте:
1. Версию Node.js (рекомендуется 18+)
2. Подключение к базе данных
3. Логи в терминале при запуске `npm run dev`

